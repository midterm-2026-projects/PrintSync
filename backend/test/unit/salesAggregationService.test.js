import { describe, it, expect } from "vitest";

describe("salesAggregationService (legacy file)", () => {
  it("is deprecated; use salesAnalyticsService.test.js", () => {
    expect(true).toBe(true);
  });
});
